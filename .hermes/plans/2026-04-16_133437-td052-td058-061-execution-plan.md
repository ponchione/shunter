# TD-052 + TD-058..TD-061 execution plan

## Goal
Close all currently mentioned debt items:
- TD-052 (commitlog `OpenSegment` filename-parse handling)
- TD-058 (`ReconcileJoinDelta` bag-netting correctness)
- TD-059 (subscription eval error over-dropping connections)
- TD-060 (join-placement tiering gap for join-bearing predicates)
- TD-061 (join bootstrap only probing RHS index during `initialQuery`)

## Current context
- TD-052 already appears fixed locally and `rtk go test ./commitlog` passes.
- Subscription package already has live tests and existing dirty worktree changes, so edits must stay narrow and avoid unrelated churn.
- Relevant spec surfaces:
  - SPEC-004 Story 3.4 bag-semantic dedup
  - SPEC-004 Story 2.4 index placement
  - SPEC-004 Story 4.2 registration / initial query
  - SPEC-004 Story 5.1 eval error handling

## Approach
1. Verify TD-052 remains resolved and keep it in the completion set.
2. For each subscription debt item, follow bugfix workflow:
   - reproduce / inspect root cause
   - add or extend a regression test first
   - run the targeted test to watch it fail
   - implement the smallest fix
   - run targeted package tests
   - update TECH-DEBT status once verified
3. Finish with broader verification over touched packages.

## Planned steps
1. Verify current TD-052 delta and avoid regressing it.
2. TD-058:
   - inspect `subscription/delta_dedup.go` and current tests
   - add an order-sensitive regression where insert/delete multiplicities net differently by fragment order
   - fix reconciliation to use one signed net tally and materialize from sign
3. TD-059:
   - inspect `handleEvalError` and spec/docs
   - add regression covering one broken query plus another healthy subscription on the same connection
   - narrow cleanup signaling so evaluation errors queue `SubscriptionError` without disconnecting whole connections
4. TD-060:
   - inspect `PlaceSubscription` / `RemoveSubscription` and candidate collection behavior for join predicates with same-side filter placement
   - add regression proving RHS-only changes miss the query before the fix
   - update placement logic so join-bearing predicates get the needed join-edge placement while preserving per-table semantics and symmetric removal
5. TD-061:
   - inspect `initialQuery` join bootstrap and resolver behavior
   - add regression where validation passes via only LHS index but RHS index is absent
   - implement fallback bootstrap path that scans RHS and probes LHS when only LHS index exists
6. Update `TECH-DEBT.md` for resolved items.
7. Run `rtk go test ./subscription ./commitlog` and, if clean, a broader repo check if low-risk.

## Likely files to change
- `subscription/delta_dedup.go`
- `subscription/delta_dedup_test.go`
- `subscription/eval.go`
- `subscription/eval_test.go`
- `subscription/placement.go`
- `subscription/placement_test.go`
- `subscription/register.go`
- `subscription/manager_test.go`
- `TECH-DEBT.md`
- possibly no further TD-052 code changes unless verification finds drift

## Risks / watchpoints
- SPEC text for eval errors mentions disconnecting clients, but current debt item argues this is too broad relative to subscription-scoped failure handling. Keep the code change narrowly aligned with existing `SubscriptionError` transport and avoid breaking fan-out worker assumptions.
- Placement changes must preserve registration/removal symmetry and not explode candidate duplication.
- Join bootstrap fallback must not silently swallow resolver/schema mismatches when neither side resolves.

## Verification
- `rtk go test ./commitlog`
- `rtk go test ./subscription`
- targeted runs for new regressions while iterating
