# Phase 1 audit-fix slice plan

> For Hermes: execute this as one narrow Shunter reconciliation slice with strict TDD and RTK-only shell commands.

Goal: close the concrete audit findings left after Phase 1 wire-level parity so the repo’s parity pins and touched protocol docs are accurate and trustworthy.

Architecture: keep the slice narrow. Strengthen the parity-message-family pin tests so they assert the current wire shapes directly, then make the smallest doc repairs needed to align touched protocol/status docs with the live code. Also repair the stale focused-test command in the Phase 1 implementation plan so the repo’s own plan remains executable.

Tech stack: Go test suite under `protocol/`, markdown docs under `docs/` and `.hermes/plans/`, RTK for all shell commands.

Files in scope:
- Modify: `protocol/parity_message_family_test.go`
- Modify: `docs/current-status.md`
- Modify: `docs/decomposition/005-protocol/SPEC-005-protocol.md`
- Modify: `.hermes/plans/2026-04-18_073534-phase1-wire-level-parity.md`
- Optional follow-through if wording needs tightening after test changes: `docs/parity-phase0-ledger.md`, `SPEC-AUDIT.md`

Task 1: strengthen weak Phase 1 deferral pins
1. Write failing tests first by tightening the existing tests in `protocol/parity_message_family_test.go` so they assert exact field-name order for `SubscribeMsg`, `CallReducerMsg`, and `OneOffQueryMsg`, and exact concrete field type for `ReducerCallResult.Status`.
2. Run focused tests with `rtk go test ./protocol -run 'TestPhase1Deferral(Subscribe|CallReducer|OneOffQuery|ReducerCallResult)' -v` and verify RED.
3. Implement the minimum helper/test updates needed so the tests express the stronger invariants without widening scope.
4. Re-run the same focused tests and verify GREEN.

Task 2: repair touched docs to match live protocol state
1. Update `docs/current-status.md` protocol bullets so they no longer claim the subprotocol namespace and compression-tag behavior still differ after Phase 1.
2. Update `docs/decomposition/005-protocol/SPEC-005-protocol.md` divergence bullet for close-code policy so it acknowledges `1002` protocol-error use in live code.
3. Update the stale focused test command in `.hermes/plans/2026-04-18_073534-phase1-wire-level-parity.md` to point at the actual subprotocol parity test names/pattern.
4. If the strengthened parity tests require wording updates in `docs/parity-phase0-ledger.md` or `SPEC-AUDIT.md`, make the narrow matching doc edits only.

Task 3: verify end-to-end
1. Run `rtk go test ./protocol -run 'TestPhase1Deferral|TestPhase1Parity' -v`.
2. Run `rtk go test ./protocol`.
3. Run `rtk go test ./...`.
4. Run `rtk git status --short` and confirm the worktree only contains the intended slice changes.

Completion criteria:
- Phase 1 deferral pins fail if current wire shapes drift in the ways the audit identified.
- `docs/current-status.md` and `docs/decomposition/005-protocol/SPEC-005-protocol.md` match the live Phase 1 protocol behavior.
- The saved Phase 1 plan’s focused subprotocol test command is executable against the current test names.
- Focused protocol tests, package tests, and full suite all pass.
