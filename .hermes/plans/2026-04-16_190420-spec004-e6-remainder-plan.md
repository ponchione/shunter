# SPEC-004 E6 remainder: Fan-Out & Delivery

## Goal
Close SPEC-004 E6 as an implemented slice by verifying the live runtime wiring, reconciling stale subscription docs/spec text with the shipped code, and re-running the fan-out/protocol/executor/commitlog validation suite.

## Current grounded context
- Live code already contains the expected E6 runtime pieces:
  - `subscription/FanOutWorker` with confirmed-read gating, caller diversion, dropped-client signaling
  - executor `PostCommitMeta` wiring with `TxDurable`, caller connection metadata, and external-only caller results
  - commitlog `WaitUntilDurable(txID)` readiness channels
  - protocol fan-out adapter and reducer-result delivery helpers
- Validation already passes:
  - `rtk go test ./subscription/... ./protocol/... ./executor/... ./commitlog/...`
  - `rtk go test -race ./subscription/... ./protocol/... ./executor/...`
  - `rtk go vet ./subscription/... ./protocol/... ./executor/... ./commitlog/...`
- Remaining gap is documentation drift inside the SPEC-004 docs:
  - `PHASE-5-DEFERRED.md` still says Epic 6 remainder is deferred
  - Story/spec text still references `ClientSender` / `ErrClientBufferFull` and “no mutex needed”, while live code uses a `FanOutSender` seam, `ErrSendBufferFull`/`ErrSendConnGone`, and a mutex because mutators are called from outside the worker loop
  - SPEC-004 §10.1 still omits `meta PostCommitMeta`

## Approach
1. Verify the live code satisfies the Epic 6 remainder contracts and note the exact implementation seams.
2. Update only SPEC-004-scope docs to match the real implementation without expanding scope into unrelated specs.
3. Re-run the required validation commands after doc edits to confirm the codebase remains green and the slice can be reported complete.

## Concrete steps
1. Inspect the live `subscription`, `executor`, `commitlog`, and `protocol` files relevant to E6 follow-through.
2. Update `docs/decomposition/004-subscriptions/PHASE-5-DEFERRED.md` to remove the stale Epic 6 deferral and mark the follow-through complete.
3. Update `docs/decomposition/004-subscriptions/epic-6-fanout-delivery/story-6.1-fanout-worker.md`:
   - name the `FanOutSender` seam
   - document shared dropped-channel wiring
   - replace the stale “no mutex needed” note with the real race-safe policy
4. Update `docs/decomposition/004-subscriptions/epic-6-fanout-delivery/story-6.3-backpressure.md`:
   - align send/error examples with `ErrSendBufferFull`
   - note the subscription-side sentinel mapping from protocol errors
5. Update `docs/decomposition/004-subscriptions/SPEC-004-subscriptions.md`:
   - reflect `FanOutSender` terminology in §8
   - add `Errors` handling to the fan-out algorithm
   - extend `SubscriptionManager.EvalAndBroadcast(..., meta PostCommitMeta)` in §10.1
   - declare `PostCommitMeta`, `SubscriptionError`, and the forward `ReducerCallResult` shape in the interface section
6. Re-run validation:
   - `rtk go test ./subscription/... ./protocol/... ./executor/... ./commitlog/...`
   - `rtk go test -race ./subscription/... ./protocol/... ./executor/...`
   - `rtk go vet ./subscription/... ./protocol/... ./executor/... ./commitlog/...`

## Files likely to change
- `.hermes/plans/2026-04-16_190420-spec004-e6-remainder-plan.md`
- `docs/decomposition/004-subscriptions/PHASE-5-DEFERRED.md`
- `docs/decomposition/004-subscriptions/SPEC-004-subscriptions.md`
- `docs/decomposition/004-subscriptions/epic-6-fanout-delivery/story-6.1-fanout-worker.md`
- `docs/decomposition/004-subscriptions/epic-6-fanout-delivery/story-6.3-backpressure.md`

## Validation / exit criteria
- SPEC-004 docs no longer claim Epic 6 remainder is deferred.
- SPEC-004 fan-out docs match the live `FanOutSender`/`PostCommitMeta`/durability-ready-channel design.
- Required go test / race / vet commands pass.

## Risks
- Over-editing beyond the SPEC-004 slice. Avoid touching unrelated specs unless an exact E6 contract reference requires it.
- Accidentally contradicting live code. Keep every doc change anchored to inspected implementation.