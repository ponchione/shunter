{
  "name": "@shunter/browser-integration",
  "private": true,
  "type": "module",
  "scripts": {
    "install:browsers": "playwright install chromium",
    "test": "npm --prefix ../client run build && node test/strict-auth-browser.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
