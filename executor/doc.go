// Package executor serializes reducer, lifecycle, scheduler, subscription, and
// protocol-adapter work for Shunter runtimes.
//
// Executor is a runtime implementation package. Application code should prefer
// the root shunter runtime APIs, contract JSON, generated clients, or protocol
// surfaces unless it is intentionally working on Shunter internals.
package executor
